name: violet
description: A workshop flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  health: ^6.0.0
  flutter_hooks: ^0.18.6
  geolocator: ^9.0.2
  lottie: ^2.3.2
  google_maps_flutter: ^2.2.6
  google_fonts: ^4.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/